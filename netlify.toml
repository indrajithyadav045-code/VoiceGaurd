[build]
  command = "npm ci --prefix voiceguard_backend/frontend && npm run build --prefix voiceguard_backend/frontend"
  publish = "voiceguard_backend/frontend/dist"

# The React dashboard is a single-page application. Serve it for direct links.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
