[build]
  base = "voiceguard_backend/frontend"
  command = "npm run build"
  publish = "dist"

# The React dashboard is a single-page application. Serve it for direct links.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
